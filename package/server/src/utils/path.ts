import path from 'path';

export const ASSET_DIR2 = path.join(process.cwd(), 'upload', 'asset');
export const USER_DIR2 = path.join(process.cwd(), 'upload', 'user');
export const ASSET_DIR = path.join(__dirname, 'upload', 'asset');
export const USER_DIR = path.join(__dirname, 'upload', 'user');
