[Prisma Migration Guide](https://blog.logrocket.com/effortless-database-schema-migration-prisma/)

To run the migration (on dev mode only), run the following command:

```bash
npx prisma migrate dev --name cmd_why_migrate
```
